export const useApiBaseUrl = () => {
  // You can easily change this to your production URL later
  // or use useRuntimeConfig().public.apiBaseUrl if configured in nuxt.config.ts
  return 'http://localhost:3001';
};
